# Secrets
.env

# Git
.git/
.gitignore

# Python cache
__pycache__/
*.pyc
*.pyo

# index/ is intentionally included — Cloud Run is stateless so the index
# must be baked into the image. For local dev, docker-compose mounts it instead.

# Dev-only
venv/
.venv/
notebooks/
tests/
requirements-dev.txt

# Chainlit translation files (downloaded at runtime if needed)
.chainlit/translations/
